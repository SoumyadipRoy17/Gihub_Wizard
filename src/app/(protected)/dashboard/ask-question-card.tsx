"use client";
import useProject from "@/hooks/use-project";
import React from "react";

const AskQuestionCard = () => {
  const { project } = useProject();

  return <div>AskQuestionCard</div>;
};

export default AskQuestionCard;
